# Local API (Minimal)

Minimal Express API for local development.

## Endpoints

- `GET /posts`
- `GET /posts/:id`
- `POST /posts`
- `PUT /posts/:id`
- `DELETE /posts/:id`
- `GET /health`

## Setup

```bash
cd api
npm install
cp .env.example .env
npm start
```
